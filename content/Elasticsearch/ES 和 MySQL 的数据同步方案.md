+++
date = '2025-12-25T10:52:45+08:00'
draft = false
title = 'ES 和 MySQL 的数据同步方案'
+++

这篇文章的内容已经合并并扩展到 [Elasticsearch 数据同步方案](./Elasticsearch数据同步方案.md)。

保留这个文件主要是为了兼容旧链接。新的主文章已经补充了：

- 全量同步、增量同步、Outbox、Binlog 监听的方案对比。
- Bulk 导入、refresh 控制、alias 切换。
- 幂等写入、失败重试、死信队列。
- 消息重复、消息乱序、消息丢失的处理方式。
- 多表宽表同步和定时对账补偿。

如果是为了系统学习，建议直接阅读合并后的版本。重复内容如果还留在两篇文章里，只会让读者在两个相似标题之间来回横跳。这种体验并不高明，所以这里就不再保留旧正文了。
